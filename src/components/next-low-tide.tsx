import { Card, CardHeader, CardTitle } from "./ui/card";

export const NextLowTide = () => {
  return (
    <Card>
      <CardHeader className="flex flex-row justify-center">
        <CardTitle className="text-2xl">Next low tide in...</CardTitle>
      </CardHeader>
    </Card>
  );
};
